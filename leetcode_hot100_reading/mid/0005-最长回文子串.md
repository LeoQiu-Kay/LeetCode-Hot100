# 5. 最长回文子串 · 阅读版

> 难度：中等 · [LeetCode 链接](https://leetcode.cn/problems/longest-palindromic-substring/)

---

## 一、题目讲了什么

找出字符串里**最长的回文子串**。

例：`"babad"` → `"bab"` 或 `"aba"`（都长 3）；`"cbbd"` → `"bb"`。

回文 = 正反读一样；子串 = 连续片段。

---

## 二、思路对比

| 方法 | 时间 | 空间 | 评价 |
|---|---|---|---|
| 枚举所有 (i, j) 判回文 | O(n³) | O(1) | 太慢 |
| 区间 DP（`dp[i][j]` 是否回文） | O(n²) | O(n²) | 标准 DP，但实现繁 |
| **中心扩展** | O(n²) | O(1) | 简单优雅，推荐 |
| Manacher | O(n) | O(n) | 神算法，hot100 用不上 |

讲**中心扩展**。

---

## 三、中心扩展的关键观察

每个回文都有一个"**中心**"。回文长度有两类：

- **奇数长度**：中心是**一个字符**（如 `"aba"`，中心 'b'）
- **偶数长度**：中心是**两个字符之间**（如 `"abba"`，中心 'b' 和 'b' 之间）

总共有 `2n - 1` 个候选中心（n 个奇数中心 + n - 1 个偶数中心）。对每个中心向两边**扩展**，扩展到不能扩为止——记录到的最长回文就是答案。

---

## 四、扩展函数

```
expand(l, r):
    while l >= 0 and r < n and s[l] == s[r]:
        l -= 1
        r += 1
    # 退出 while 时 s[l] != s[r] 或越界，有效回文是 s[l+1 .. r-1]
    return l + 1, r       # 返回半开区间 [l+1, r)
```

调用方式：

- 以 i 为中心（奇数长）：`expand(i, i)`
- 以 (i, i+1) 为中心（偶数长）：`expand(i, i+1)`

两次扩展取较长的更新答案。

---

## 五、写成代码

```python
class Solution:
    def longestPalindrome(self, s: str) -> str:
        def expand(l: int, r: int) -> tuple[int, int]:
            # 从 (l, r) 向两侧扩展，返回最长回文的 (起点, 终点+1)
            while l >= 0 and r < len(s) and s[l] == s[r]:
                l -= 1
                r += 1
            # 退出时 s[l]、s[r] 不匹配或越界，有效范围是 [l+1, r-1]
            return l + 1, r
        best = (0, 1)   # (起点, 终点+1)
        for i in range(len(s)):
            # 奇数长度回文（以 i 为正中央）
            a, b = expand(i, i)
            if b - a > best[1] - best[0]:
                best = (a, b)
            # 偶数长度回文（以 i, i+1 为两中央）
            a, b = expand(i, i + 1)
            if b - a > best[1] - best[0]:
                best = (a, b)
        return s[best[0]:best[1]]
```

---

## 六、复杂度

- **时间** `O(n²)`：n 个中心 × 每次 O(n) 扩展
- **空间** `O(1)`：只存最佳区间

---

## 七、容易踩的坑

1. **必须两种中心都试**：少一种就会漏一类（少了偶数中心 → 找不到 "abba"）。
2. **退出 while 后 l/r 都已经"超出一格"**，所以有效回文是 `[l+1, r-1]` 闭区间或 `[l+1, r)` 半开。
3. **不要写完整 DP**：能写更优雅就别写难调试的。

---

## 八、一句话记忆

> 枚举每个**中心**（奇数 i / 偶数 i, i+1），两侧扩展找最长。

---

## 九、可以接着想的同套路题

| 题号 | 名字 | 关系 |
|---|---|---|
| 647 | 回文子串 | 数所有回文子串数量，扩展同款（不在 Hot 100）|
| 516 | 最长回文子序列 | 区间 DP，**子序列**而不是子串 |
| 131 | 分割回文串 | 回溯 + 判回文，复用扩展或 DP 表 |
| 234 | 回文链表 | 链表版回文 |
