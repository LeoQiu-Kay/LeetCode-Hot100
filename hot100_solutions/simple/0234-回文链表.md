# 234. 回文链表

- 难度：简单
- 链接：https://leetcode.cn/problems/palindrome-linked-list/

## 直觉 / 记忆点

> 判一首诗正反读一样，但只能从头读到尾、不能跳读——三步走：1) **龟兔赛跑找中点**（兔到终点时龟到中央）；2) 把后半段链表**就地反转**；3) 从原头和反转后的后半段同时往后比，逐字对上才是回文。空间 O(1) 的关键就在"反转"那一步。

## 思路

要求 O(1) 空间，三步走：
1. **快慢指针**找到中点（慢指针走到中间）；
2. **反转**后半段链表；
3. 从头和反转后的后半段同时往后比较值，全相等就是回文。

## 代码

```python
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

class Solution:
    def isPalindrome(self, head: ListNode | None) -> bool:
        # 1. 找中点
        slow = fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
        # 2. 反转后半段
        prev = None
        while slow:
            slow.next, prev, slow = prev, slow, slow.next
        # 3. 比较
        while prev:
            if prev.val != head.val:
                return False
            prev = prev.next
            head = head.next
        return True
```

## 复杂度

- 时间：O(n)
- 空间：O(1)

## 易错点

- 偶数长度时慢指针落在中右；奇数时落在正中央。两种情况下反转后两段长度差最多 1，**用反转后那段作为循环终止条件**即可避免越界。
- 用列表 `vals = [...]; return vals == vals[::-1]` 也能过，但是 O(n) 空间；面试要写 O(1)。
