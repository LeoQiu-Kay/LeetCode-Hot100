# 55. 跳跃游戏

- 难度：中等
- 链接：https://leetcode.cn/problems/jump-game/

## 直觉 / 记忆点

> 一边走一边维护"目前能到的最远位置 `reach`"。如果某一步发现当前位置 i > reach（reach 还没到 i），说明卡死了，返回 False。能走到 n-1 就成功。

## 思路

一次遍历，更新 reach = max(reach, i + nums[i])。

## 代码

```python
class Solution:
    def canJump(self, nums: list[int]) -> bool:
        reach = 0   # 目前能到达的最远下标
        for i, x in enumerate(nums):
            # 当前位置 i 都到不了，卡死
            if i > reach:
                return False
            # 从 i 出发能到的最远位置是 i + x，更新 reach
            reach = max(reach, i + x)
            # 优化：已能到末尾，可以提前 return True
            if reach >= len(nums) - 1:
                return True
        return True
```

## 复杂度

- 时间：O(n)
- 空间：O(1)

## 易错点

- 判断卡死必须**在更新 reach 之前**：先看 `i > reach` 再更新，否则当前位置不可达却还更新 reach 是错的。
- 提前 return True 可以省时间，但不是必需。
