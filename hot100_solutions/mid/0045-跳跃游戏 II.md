# 45. 跳跃游戏 II

- 难度：中等
- 链接：https://leetcode.cn/problems/jump-game-ii/

## 直觉 / 记忆点

> 维护两个变量：**当前跳的边界 `end`**（这一跳能到的最远）、**下次能到的最远 `farthest`**。每走一步更新 farthest；当 i 走到 end 时表示这一跳必须结束，跳数 +1、把 end 推到 farthest。

## 思路

一次遍历，遇到当前跳边界就"换跳"。

## 代码

```python
class Solution:
    def jump(self, nums: list[int]) -> int:
        jumps = 0       # 已跳次数
        end = 0         # 当前这一跳能到的最远下标
        farthest = 0    # 下一跳能到的最远下标
        # 不需要遍历最后一个：到最后一个就不用再跳了
        for i in range(len(nums) - 1):
            # 更新"下一跳的最远"
            farthest = max(farthest, i + nums[i])
            # 走到当前跳的边界 → 必须发起下一跳
            if i == end:
                jumps += 1
                end = farthest
        return jumps
```

## 复杂度

- 时间：O(n)
- 空间:O(1)

## 易错点

- 循环只到 `len(nums) - 1`：因为走到最后一个时不需要再触发新跳。
- 题目保证一定能到末尾，不用判断卡死。
