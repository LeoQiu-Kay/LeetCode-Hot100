# 438. 找到字符串中所有字母异位词

- 难度：中等
- 链接：https://leetcode.cn/problems/find-all-anagrams-in-a-string/

## 直觉 / 记忆点

> **定长**滑动窗口（窗口 = len(p)）。维护窗口内字符计数与 p 的字符计数是否相等。每滑一格：右进一个字符（+1）、左出一个字符（-1）。可以用两个数组直接比较，也可以维护"还差几种字符"的计数加速。

## 思路

26 长度计数数组 cnt_p、cnt_w；先初始化首个窗口；然后滑动比较。

## 代码

```python
class Solution:
    def findAnagrams(self, s: str, p: str) -> list[int]:
        ns, np = len(s), len(p)
        if ns < np:
            return []
        # 26 长度的字符计数数组（小写字母）
        cnt_p = [0] * 26
        cnt_w = [0] * 26
        for c in p:
            cnt_p[ord(c) - ord('a')] += 1
        ans: list[int] = []
        for i, c in enumerate(s):
            # 右端字符进窗口
            cnt_w[ord(c) - ord('a')] += 1
            # 窗口长度超过 p 时把左端字符踢出
            if i >= np:
                cnt_w[ord(s[i - np]) - ord('a')] -= 1
            # 窗口长度恰为 np 且与 p 的计数完全相同：找到一个异位词
            if i >= np - 1 and cnt_w == cnt_p:
                # 起点是 i - np + 1
                ans.append(i - np + 1)
        return ans
```

## 复杂度

- 时间：O(N · Σ)，N = len(s)，Σ = 26（数组比较常数）
- 空间：O(1)

## 易错点

- 窗口起点 = `i - np + 1`，别忘了 +1。
- 长度判断：`i >= np` 时才需要踢左端字符；`i >= np - 1` 时才开始比较（窗口刚装满）。
