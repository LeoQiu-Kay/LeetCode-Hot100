# 139. 单词拆分

- 难度：中等
- 链接：https://leetcode.cn/problems/word-break/

## 直觉 / 记忆点

> `dp[i]` 表示 `s[:i]` 是否能拆。转移：枚举上一个切点 j，若 `dp[j]` 为真且 `s[j:i]` 在词典里，则 `dp[i]` 为真。

## 思路

dp 长度 n+1，dp[0] = True（空串）。词典用 set 加速 in 查询。

## 代码

```python
class Solution:
    def wordBreak(self, s: str, wordDict: list[str]) -> bool:
        # 词典转 set，in 查询 O(1)
        words = set(wordDict)
        n = len(s)
        # dp[i] 表示 s[:i] 是否可拆；dp[0] = True 表示空串可拆
        dp = [False] * (n + 1)
        dp[0] = True
        for i in range(1, n + 1):
            # 枚举本段的起点 j（即上一段的终点）
            for j in range(i):
                # 前 j 个字符可拆 + s[j:i] 在词典中
                if dp[j] and s[j:i] in words:
                    dp[i] = True
                    break   # 一个能拆就够了
        return dp[n]
```

## 复杂度

- 时间：O(n² · L)，L 是平均单词长（切片 + set 查询）
- 空间：O(n + 词典)

## 易错点

- dp[0] = True 是基础情况（空串可拆），不设的话整个 dp 永远是 False。
- 内层找到一个就 break，常数能省一半。
